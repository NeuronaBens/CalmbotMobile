class Settings {
  int id;
  bool dataCollection;
  String theme;
  int studentId;

  Settings(
      {required this.id,
      required this.dataCollection,
      required this.theme,
      required this.studentId});
}
